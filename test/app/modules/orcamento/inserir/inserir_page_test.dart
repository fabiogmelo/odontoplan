import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('InserirPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(InserirPage(title: 'Inserir')));
    //  final titleFinder = find.text('Inserir');
    //  expect(titleFinder, findsOneWidget);
  });
}
