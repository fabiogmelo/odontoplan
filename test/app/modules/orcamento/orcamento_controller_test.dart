import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:odontoplan/app/modules/orcamento/orcamento_module.dart';

void main() {
  initModule(OrcamentoModule());
  // OrcamentoController orcamento;
  //
  setUp(() {
    //     orcamento = OrcamentoModule.to.get<OrcamentoController>();
  });

  group('OrcamentoController Test', () {
    //   test("First Test", () {
    //     expect(orcamento, isInstanceOf<OrcamentoController>());
    //   });

    //   test("Set Value", () {
    //     expect(orcamento.value, equals(0));
    //     orcamento.increment();
    //     expect(orcamento.value, equals(1));
    //   });
  });
}
