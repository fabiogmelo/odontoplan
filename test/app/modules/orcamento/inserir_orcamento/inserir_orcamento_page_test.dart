import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('InserirOrcamentoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(InserirOrcamentoPage(title: 'InserirOrcamento')));
    //  final titleFinder = find.text('InserirOrcamento');
    //  expect(titleFinder, findsOneWidget);
  });
}
