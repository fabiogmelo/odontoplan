import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:odontoplan/app/modules/orcamento/inserir_orcamento/inserir_orcamento_module.dart';

void main() {
  initModule(InserirOrcamentoModule());
  // InserirOrcamentoController inserirorcamento;
  //
  setUp(() {
    //     inserirorcamento = InserirOrcamentoModule.to.get<InserirOrcamentoController>();
  });

  group('InserirOrcamentoController Test', () {
    //   test("First Test", () {
    //     expect(inserirorcamento, isInstanceOf<InserirOrcamentoController>());
    //   });

    //   test("Set Value", () {
    //     expect(inserirorcamento.value, equals(0));
    //     inserirorcamento.increment();
    //     expect(inserirorcamento.value, equals(1));
    //   });
  });
}
