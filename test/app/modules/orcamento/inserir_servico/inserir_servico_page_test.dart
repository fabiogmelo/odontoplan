import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('InserirServicoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(InserirServicoPage(title: 'InserirServico')));
    //  final titleFinder = find.text('InserirServico');
    //  expect(titleFinder, findsOneWidget);
  });
}
