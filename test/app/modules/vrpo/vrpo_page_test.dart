import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('VrpoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(VrpoPage(title: 'Vrpo')));
    //  final titleFinder = find.text('Vrpo');
    //  expect(titleFinder, findsOneWidget);
  });
}
